# 更新日志

本文只记录玩家、服主、地图作者或兼容性维护者能够感知的变化。内部调查过程记录在 `docs/WORKLOG.md`；纯重命名和无行为变化的机械整理一般只写工作日志。

格式遵循“未发布 / 版本号”结构。每个条目放入“新增、变更、修复、性能、兼容性、移除、安全”之一，并尽量附带可复现条件或迁移说明。

## [未发布]

### 新增

- 建立 1.3.0 冻结行为规格、2.0 重构计划、架构说明、测试矩阵和持续工作日志。

### 变更

- 重构工作迁移到独立目录 `ZYLPVPMOD2.0` 和分支 `codex/refactor-2.0`；原 `ZYLPVPMOD` 目录冻结。

### 修复

- 修正大厅好友检测函数把 `false` 拼成 `fasle`，导致未匹配好友时返回值不明确的问题。
- 大厅上下文关闭时成对注销全部全局 Events/LuaEvents，避免热重载或 Shell 往返后旧上下文继续接收联机事件。
- 掉线冻结只保存第一份单位移动力快照，重复掉线通知不会用已经归零的移动力覆盖原快照；重连时按单位 ID 恢复到精确值。
- 修复重开投票面板中 `b_RemapArmed` 大小写不一致产生的意外全局变量，并阻止非房主路径触发本地主机重开。
- 重同步种子消息在数值格式错误时不再误触发目标玩家重同步；移除一段依赖未定义本地状态变量、实际永远不会工作的旧检查协议。
- 突然死亡计时器拒绝非数字/非正数调整和无效淘汰目标，避免畸形房主消息把本地计时状态写成空值。
- 主回合计时器不再调用从未定义的启动门状态和函数；非联机同步回合上下文不会再更新处理面板状态，畸形时间调整事件会被拒绝。
- 主机设置界面关闭时会注销全部前端 Events/LuaEvents；完成内容配置的匿名监听改为可注销监听，避免热重载后重复响应。
- 断线控制器以状态转换处理通知：重复断线/重连不再重复触发冻结或恢复，玩家再次断线时重新从 0 计算本次离线时间；Context 关闭会撤销由本组件申请的暂停。
- 主菜单 Context 关闭时完整注销服务器状态、云回合、内容配置、账号、窗口和 Lua 导航监听，避免前端热重载后重复响应。
- 大厅调试日志不再为显示下一名玩家而额外调用三次有状态的 `GetNextID()`，避免调试路径重复广播 PlayerInfo；`.broadcast_player_0` 调试命令不再把玩家 0 昵称写成 `paf`。

### 性能

- 把大厅 `QuickRefresh` 从每次 `GameCoreEventPublishComplete` 回调降为按当前 1–5 秒阶段间隔执行；GameConfig 变化仍立即刷新。
- 隐藏能力标志和 Ban 槽只在值实际变化时写入，减少配置事件级联；版本握手轮询日志只在调试模式输出。
- 移除 Multiplayer Helper 每回合仅为日志而消耗一次同步游戏随机数的路径；重连单位恢复由双重循环改为线性索引。
- 重开执行消息的三次 GameConfig/两次 PlayerInfo 广播收敛为各一次；重开完成监听仅在等待快照时注册，完成后立即注销。
- 重同步超时由两个常驻高频事件改为仅重同步期间启用的单一 UI 事件，并限制为每秒一次；房主对本轮客户端复用一次地图指纹，避免逐客户端重新扫描全图。
- 突然死亡剩余时间从“每次玩家激活都向所有客户端发送”改为每个游戏回合只同步一次；计时完成后通过统一生命周期函数注销 Tick。
- 主回合计时器缓存单次公式计算所需的模式值，只在计时秒数或计时器类型实际变化时广播配置；重复 `p+++`/`p++++` 不再重复关闭计时器或发送配置。
- 刷新地图/游戏种子只广播实际变化的游戏配置，不再额外广播无关的玩家槽信息；移除主机设置界面中不会成立的自检扫描和随机写配置分支。
- 大厅原有 88 个活跃 `print` 调用点中，启动日志已删除，其余 87 个统一进入默认关闭的调试日志；只有房主显式启用现有 `.debug` 模式后才输出，降低投票、Ban/Pick、槽位和自动开局流程的正式日志 I/O。
- 房主“随机分队”和批量关闭空槽改为先完成全部玩家配置变更、再统一广播一次 PlayerInfo；广播次数分别由参与者数/空槽数降为固定 1 次，避免客户端看到批处理中间状态。

### 安全

- 多人选项不再把收到的每条聊天原文写入 Lua 日志；请求快照、测试同步和强制重同步三个调试聊天入口默认关闭。
- 主菜单的本地 Mod 版本探测和离开会话诊断默认不再写入正式日志。

### 兼容性

- 固定文本文件的 Git 行尾策略，二进制美术资源不再参与文本规范化。

### 移除


## [1.3.0] - 2026-09-06

### 新增

- 以提交 `1852e1d4ddc5abcfc4507885fc1141866a9154e0` 和标签 `v1.3.0` 冻结重构前基线。
- 1.3.0 的完整行为清单见 `ZYLPVPMOD1.3.0修改大全.md`。

[未发布]: https://github.com/gitZYLhub/ZYLCIV6PVP/compare/v1.3.0...codex/refactor-2.0
[1.3.0]: https://github.com/gitZYLhub/ZYLCIV6PVP/releases/tag/v1.3.0
